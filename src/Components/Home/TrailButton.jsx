import React from "react";

const ChatBot = () => {
  const handleButtonClick = () => { };

  return (
    <button className="trail-button" onClick={handleButtonClick}>
      Free 7 Day Trail
    </button>
  );
};

export default ChatBot;
